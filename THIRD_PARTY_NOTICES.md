# Third-Party Notices

This Windows-only source release does not include vendor camera SDK binaries,
private drivers, signing certificates, or packaged third-party binary
frameworks.

The Windows camera implementation uses Windows platform APIs through .NET and
DirectShow COM interop declarations in source form.

If future work adds bundled third-party binaries or SDKs, document their
license terms here before publishing them.

